# Lindenmayer Garden

L-Systems in Rust

## Compile documents
Install `tectonic`
```bash
chmod +x compile
./compile abstract
./compile documentation
./compile diaries
```
This will generate `diaries.pdf`, `abstract.pdf`
and `documentation.pdf` in `mandate`.